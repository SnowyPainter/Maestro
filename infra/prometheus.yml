global:
  scrape_interval: 15s
  scrape_timeout: 10s

scrape_configs:
  - job_name: "maestro-sidecar"
    metrics_path: /metrics
    static_configs:
      - targets:
          - "host.docker.internal:9600"

  - job_name: "maestro-backend"
    metrics_path: /metrics
    static_configs:
      - targets:
          - "host.docker.internal:8000"
