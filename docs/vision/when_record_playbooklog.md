# Playbook Logging Triggers

- email replied (`event = email.replied`)
- schedule created (`event = schedule.created`)
- schedule cancelled (`event = schedule.cancelled`)
- post published (`event = post.published`)
- post deleted (`event = post.deleted`)
- sync metrics (`event = sync.metrics`)
