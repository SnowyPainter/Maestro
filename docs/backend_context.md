## 목표

- 요청 단위로 **누가**(사용자/계정), **어디서**(엔드포인트), **무엇을**(액션), **얼마**(LLM 토큰/비용) 했는지 끝까지 추적.
- FastAPI → Celery 워커 → LLM 호출 → /internal/events 콜백까지 동일한 컨텍스트 키로 연결.
- 모든 상태 전이는 오케스트레이터로 귀환해 단일 창구(SSOT) 에서 로깅·검증·멱등·감사를 처리.